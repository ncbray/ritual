import foo;

extern fn print_int(value:i32);

fn swap(a:i32, b:i32) -> (i32, i32) {
    (b, foo.passthrough(a))
}

fn main() {
    let a = 3;
    let b = 3;
    let i = 0;
    while i < 5 {
        (a, b) = swap(a-1, b+b);
        print_int(a);
        print_int(b);
        i = i + 1
    }
}