import foo;

extern fn print_int(value:i32);

fn swap(a:i32, b:i32) -> (i32, i32) {
    (b, foo.passthrough(a))
}

fn main() {
    let a = 7;
    let b = 13;
    let i = 0;
    (a, b) = swap(a, b);
    print_int(a);
    print_int(b)
}